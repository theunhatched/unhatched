This sets up and updates our AWS configuration with new stuff. Ideally, you wouldn't ever go into the AWS console, and instead would just do some of the following:

```
terraform init
terraform plan
terraform apply
```
